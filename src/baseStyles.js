const baseStyles = {
  base: {
    display: 'flex',
    justifyContent: 'center',
    flexDirection: 'row',
  },

  wrapper: {
    width: 500,
    background: 'white',
  }
}

export default baseStyles
